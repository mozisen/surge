# v3.7.0-preview.6 本地预览记录

- 修复统计核心构建白名单错误拦截 Sing-box 1.14.0 正式版的问题。
- 自动构建支持范围扩展为 1.10–1.14 正式版；规范化可选的 `v` 前缀，仍拒绝预发布、格式错误及未适配的版本，不自动降级。
- 拒绝版本时显示实际核心版本输出，方便定位。
- 保留备份、配置兼容性检查、失败恢复及 `f` 显式重试流程。之前的失败状态不会自动触发再次构建；更新后需进入用户管理选择 `f` 并确认。

## 验证

- 从 [官方 v1.14.0 源码](https://github.com/SagerNet/sing-box/tree/v1.14.0) 成功构建含 `with_v2ray_api` 的 macOS/arm64 核心；使用 Go 1.25.7，与服务端构建启动工具链一致。
- 该版本实际 VLESS、AnyTLS、Trojan 本机回环流量均产生对应用户计数，gRPC 重置后归零。
- Bash 语法、Git diff 检查、五套离线回归测试通过；新增 1.14.0/v1.14.0 接受测试及非法版本拒绝测试。
- Linux 服务器构建、重启/回滚与 Reality 握手未在本次本机测试中覆盖，不能保证用户机器上的下载或资源条件满足构建要求。

仅本地记录并推送预览分支，不创建 GitHub Release。
