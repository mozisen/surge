# v3.7.0-preview.5 本地预览记录

- 修复准备阶段完成后清屏导致构建错误不可见的问题。
- 持久保存修复尝试状态；失败或取消后不再每次进入自动询问构建。成功状态仍以当前核心、配置及 API 的实际健康检查为准，不会仅凭曾经确认就当作成功。
- 用户管理新增 `f`：查看最近 40 行日志，并明确重试统计修复。
- 日志保存在 `/etc/vless-reality/singbox-stats-repair.log`（权限 600）；构建失败记录阶段及退出码。使用项目统一 SHA-256 方法兼容缺少 sha256sum 的环境。
- 手动同步在统计准备失败时停止并给出重试路径，不再继续执行注定失败的 Sing-box 同步。

截图只能证明构建未成功，不能确认服务器的具体构建失败原因。此预览修复可见性和重复询问，实际核心仍需构建并通过 API 验证才能开始统计。若 `f` 重试失败，请提供日志末尾的构建错误，注意删除可能包含的个人配置。

验证：Bash 语法检查、五套离线回归测试；保留同版本核心真实回环协议测试。Linux 服务器的构建失败尚未复现，不能声称本次已修复该服务器所有统计问题。

不创建 GitHub Release，仅本地记录、提交并推送预览分支。
