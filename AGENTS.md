# 项目协作规则

1. 每次改动完成后，都必须创建一个对应的本地 Git commit，以便后续追踪和回滚。提交仅包含本次任务的改动，不包含用户无关的未提交文件。
2. 每次改动后，都必须确保所有测试和验证通过，再提交并交付。如果因环境限制无法完成某项验证，必须明确说明未验证的内容和原因，不得声称全部通过。
3. 所有预览版均不需要创建或更新 GitHub Release（Release list）。预览版只在本地记录版本、改动内容和测试结果，并将测试所需代码推送至 GitHub 预览分支，提供对应的 GitHub 测试链接及安装命令即可；不得将预览版标记为最新正式版。
